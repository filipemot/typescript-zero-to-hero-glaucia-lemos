const a: any = 888;
const b: any = ['perigo'];
const c = a + b;
console.log(c);

const frase = 'Oi tudo bem';
console.log(frase);

const formulario: { [campoFormulario: string]: any } = {
  nome: 'Filipe',
  idade: 36,
  sobrenome: 'Mota',
};

console.log(formulario);
